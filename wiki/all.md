# Project Leek🌱
<!-- cat wiki/all.md | egrep "<a\sid=.+>" | sed -e 's/<a id=\"//' | sed -e 's/\"><\/a>//' | awk '{print "- ["$0"](#"$0")"}' -->
<a id="contents"></a>
# Содержание
- [contents](#contents)
- [introducing](#introducing)
- [main](#main)
- [master-controller](#master-controller)
- [router](#router)
- [interface](#interface)
- [connection-to-leek](#connection-to-leek)
- [uri-schema](#uri-schema)
- [protocols](#protocols)
- [y3ip](#y3ip)
- [y4sm](#y4sm)
- [y3cm](#y3cm)
- [y4sl](#y4sl)
- [y7mm](#y7mm)
- [y7lm](#y7lm)
- [extention](#extention)
- [dns](#dns)
- [fed](#fed)
- [leek-master](#leek-master)
- [master](#master)

<a id="introducing"></a>
# Введение
Leek - проект, представляющий собой коллекцию протоколов l3+ уровня, целью которого является создание гибридной сети (централизованная - классическая иерархический вертикальная топология и децентрализованная - мэш сети со связями между ними). Проект предусматривает как работу в оверлейном режиме поверх tcp/udp и т.д., так и работу напрямую с устройствами

<a id="main"></a>
# Основные понятия

<a id="master-controller"></a>
## Мастер контроллер
Мастер-контроллер - главная часть узла. Осуществляет обработку пакета. Связывает внутренний адрес пакета с конкретным интерфейсом.

### Interfaces
Представляет собой словарь, где ключом является его номер, а значеием - сокет для чтения/записи
- При отправке пакета в начало добалявяется байт с номером iid - внутренний id (0 если не используется)
- По стандарту контроллер с помощью select (interfaces.values()) достает пакет, обрабатывает его, генерирует/обновляет пакет и отправляет его с помощью роутера на нужный интерфейс

### Communicators
Представляет собой словарь, где ключом является внутренний адрес (uint16, uint16), а значнием - пара (interface_id, iid)

### Router
Описан далее

### Meta
Словарь с данными контроллера. Хранит:
- local_addresses: массив с собственными адресами (в том числе loopback)
- TODO: добавить автоматические loopback адреса для внеутренних и внешних leek адресов

<a id="router"></a>
## Router
Роутер - вторая главная часть узла. Осуществляет маршрутизацию пакета путем выбора следующего узла. Следующий узел возвращается в формате внутреннего адреса. Хранит карту в выбранном мастером мэш-сети формате (rip-like, ospf-like). Маршрутизация идёт в соответствии с hop-param

### Routetable
Таблица соответствия адреса и внутреннего номера мэш-сети. При отсутствии точного совпадения адреса от него сзади по одному отрезается подсеть и заново выполняется поиск. Если адрес отрезался до пустого, то роутер генерирует RouterException и мастер-контроллер генерирует y3cm пакет.

### Replaceaddr
Таблица соответствия адресов (если узел имеет несколько адресов) заменяет их на первый адрес (только он хранися в таблице routetable)

### Внутренние и внешние адреса
#### Внешние адреса
- 0.A[n] - локальный адрес
- A[1] - корневая мэш-сеть 
- 65534.A[128].x[n] - маршрутизация с использованием публичного ключа A(256 bytes) до ноды x[n] в этой сети
- 65535 - loopback на самого себя, переадресация на 65535.0.0
- 65535.0.0 - loopback на свой роутер
- 65535.0.1 - указатель на мастер-ноду в первой сети роутера
- 65535.1.1 - указатель на мастер-ноду во второй сети роутера
- 65535.1.0 - loopback на свой роутер во второй сети роутера (по сути равен и переадресует на 65535.0.0)
- 65535.0.x[n] - loopback на узел x[n] в первой сети роутера

#### Внутренние адреса
- 0.0 - указатель на роутер
- 1.0 - указатель на роутер из второй сети (равен и переадресуется на 0.0)
- 0.1 - указатель на мастера в первой сети
- 1.1 - указатель на мастера во второй сети (second net)
- 65534.1 - указатель на 1 родителя роутера
- 65534.0 - указатель на роутер
- 65535.3 - указатель на 3 ребенка роутера
- 65535.0 - указатель на роутер

##### Нельзя использовать адреса с длиной отличной от 2
- 1.3.2 - не указывет на 2 ребенка 3 соседа, вызывает ошибку
- 1 - просто внутренний номер сети, не является указателем ни на что (только как инфо для отладки), вызывает ошибку

<a id="interface"></a>
## Interface
Интерфейс - сопрограмма, взаимодействуюшая с одной стороны с мастер-контроллером (через unix-socket) и с другой с сокетом, с драйвером ОС, напрямую с устройством (/dev/*).

<a id="connection-to-leek"></a>
## Подключение к сети
Подключение к сети:
- Есть 2 способа связаться с узлом: стать дочерним узлом и стать соседним узлом.
- Любой узел может стать leek мастером или мастером (для этого он должен запустить соостветствующие расширения)
- К любому узлу сети (в зависимости от конфигурации сети) может подключиться новый узел (как сосед), если у узла в сети есть мастер
- К любому узлу сети может подключится узел как дочерний

<a id="uri-schema"></a>
## URI схема
URI схема и маршрутизация
- uri-schema: leek://net1.2.3.net2 // внутри leek
- uri-schema: leek://leek2@net1.12.3 // между leek2 (y4sl)

<a id="protocols"></a>
# Протоколы
<a id="y3ip"></a>
## y3ip - протокол маршрутизации
- signature: 4 bytes = "y3ip"
- version: 1 byte uint
- header_ln: 2 bytes uint
- header
    - id: 4 bytes uint
    - hop_param: 1 byte bitarray
        - min_delay: 1 bit
        - max_bandwidth: 1 bit
        - max_safety: 1 bit
        - priority: 3 bits uint
        - dont_fragment: 1 bit
        - more_fragments: 1 bit
    - fragment_offset: 2 bytes uint
    - ttl: 2 byte uint
    - nt: 1 byte uint
    - src_ln: 1 byte uint
    - src_addr: 2*src_ln bytes
    - dst_ln: 1 byte uint
    - dst_addr: 2*dst_ln bytes
    - data_ln: 2 bytes uint
- header_crc: 4 bytes uint
- data: data_ln bytes
- data_crc: 4 bytes uint

<a id="y4sm"></a>
## y4sm - транспортный протокол (обеспечение портов)
- signature: 4 bytes = "y4sm"
- src_port: 2 bytes uint
- dst_port: 2 bytes uint
- data_ln: 2 bytes uint
- data: data_ln bytes
- package_crc: 4 bytes uint

<a id="y3cm"></a>
## y3cm - протокол контрольных сообщений ::TODO::
- signature: 4 bytes = "y3sm"
- version: 1 byte uint
- header_ln: 2 bytes uint
- type: 1 byte uint
- subtype: 1 byte uint
- package_id: 4 bytes uint
- dst_ln: 1 byte uint
- dst_addr: 2*dst_ln bytes
- data_ln: 1 byte uint
- data: data_ln bytes

### Тип 0 - Эхо сообщения
- 0.0 - Эхо запрос
    - data:
        - traceroute: отправлять icmp пакеты обратно с каждого узла с timestamp
- 0.1 - Эхо ответ
    - Запрос traceroute - ответ data:
        - timestamp=<...> // src_addr берется из пакета

### Тип 1 - Недоступность сети
- 1.0 - Сеть недоступна (нет маршрута)
- 1.1 - Не используется
- 1.2 - Протокол недостижим
- 1.3 - Порт недостижим
- 1.4 - Необходима фрагментация, но она запрещена
- 1.5 - Сеть назначения недостижима (сеть в который пришло сообщение не имеет маршрута к сети)
- 1.6 - Узел назначения недостижим (сообщение дошло до конечной сети, но в ней нет такого узла)

### Тип 2 - Перенаправления
- 2.0 - Перенаправить в сеть 
- 2.1 - Перенаправит в узел
- 2.2 - Перенаправить пакет для типа обслуживания/приоритета

### Тип 3 - Время жизни
- 3.0 - TTL истек
- 3.1 - NT истек

### Тип 4 - Ошибка в пакете
- 4.0 - Ошибка в заголовке
- 4.1 - Ошибка в пакете

### Тип 255 - Пользовательские сообщение
- 255.0 - Запрос. 2 байта id, 256 байт текста
- 255.1 - Ответ. 2 байта id, 256 байт текста

<a id="y4sl"></a>
## y4sl - протокол общения между leek сетями

<a id="y7mm"></a>
## y7mm - протокол взаимодействия с мастером

<a id="y7lm"></a>
## y7lm - протокол взаимодействия с leek мастером

<a id="extention"></a>
# Дополнение
<a id="dns"></a>
## DNS
- Метка global - переопределить адрес в любой подсети, local - переопределить адрес из своей подсети

- leek://net1.2.3.net2
    1. net1 - отправляется в корневой dns сервер (с меткой global), там получается адрес (например A[n]).
    2. Далее идёт запрос адреса dns сервера в подсети A[n].2.3 у его мастера: A[n].2.3.1 (пусть адрес dns A[n].2.3.x[1]). 
    3. Идёт запрос на разрешение имени net2 (с меткой local) и получает адрес (например A[n].2.3.B[m]).
    4. Далее уже идёт запрос к адресу A[n].2.3.B[m].

- Записи dns:
    - A: net1 A[n] // заменяет имя net1 на адрес A[n]
        - А запись заменяет только простые имена, в которых нет численых чанков (например: net1 -> A[n], net2.net3 -> B[m])
        - Чанк - часть адреса/число отделенная точками: адрес 1.2.3 - чанки 1, 2 и 3
    - CNAME: net1 net2 - 
    - RA:

<a id="fed"></a>
## Федеративный картограф
- leek://leek2@net1.12.3
    1. У leek master запрашивается адрес федеративного картографа.
    2. У федеративного картографа запрашивается адрес ноды на которую надо отправить пакет
    3. Собирается l4ls (leek sailor) пакет, в который добавляется: имя родной сети, адрес в родной сети, имя удаленной сети, адрес в удаленной сети
    4. Пакет отправляется в федеративный мост, где он разбирается до l4ls, и заново формируется l3ip пакет до следующего федеративного моста
    5. По получении пакета другой сетью все повторяется
    6. По получении конечной сетью идёт dns-resolving и отправка пакета конечному узлу

<a id="leek-master"></a>
## Leek master
### Обязанности leek master
- Хранить домен первого уровня (для федеративной сети)
- Выполнять роль мастера для сети 1
- Раздавать номера сетей соседям (2-65533)
- Хранить список сервисов внутри leek сети
    - DNS - корневой dns сервер
    - Federaive-cartografer - адрес узела, хранящего карту федеративых leek сетей и их связи
        - Federative-bridge - узлы-мосты связанные с другими сетями, словарь имя_сети: адрес_узла, хранит федеративный картограф

<a id="master"></a>
## Master
